# ══════════════════════════════════════════════════════════════════════════════
#  Dharma AI Backend — Environment Variable Reference
#  ─────────────────────────────────────────────────
#  Copy this file to `.env` and fill in your values for local development.
#  NEVER commit `.env` to source control.
#
#  Variables marked [LOCAL ONLY] are ignored when APP_ENV=production.
#  Variables marked [PROD ONLY]  are only required when APP_ENV=production.
# ══════════════════════════════════════════════════════════════════════════════

# ── Application ───────────────────────────────────────────────────────────────
APP_ENV=local                           # "local" | "production"
APP_NAME="Dharma AI Backend"
APP_VERSION=1.0.0
DEBUG=true

# ── Database [LOCAL ONLY] ─────────────────────────────────────────────────────
# In production, the connection string is fetched from Azure Key Vault.
#
# For local development this connects to the Azure Cosmos DB Emulator running
# in Docker (or directly on your machine if you installed it natively).
#
# Connection string breakdown:
#   username  = "localhost"      ← emulator's fixed account name (not a hostname!)
#   password  = emulator master key (URL-encoded: / → %2F, + → %2B, = → %3D)
#   host:port = localhost:10255  ← MongoDB TLS endpoint of the emulator
#
# ⚠️  This default is for connecting from OUTSIDE Docker (bare-metal / seed script).
#     docker-compose.yml overrides the host with "cosmos-emulator" (service DNS name).
MONGODB_URL=mongodb://localhost:C2y6yDjf5%2FR%2Bob0N8A7Cgv30VRDJIWEHLM%2B4QDU5DE2nQ9nDuVTqobD4b8mGGyPMbIZnqyMcszchPhKzdKk%3D%3D@localhost:10255/dharma_db?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000&tlsAllowInvalidCertificates=true
DATABASE_NAME=dharma_db

# ── JWT ───────────────────────────────────────────────────────────────────────
# Must be at least 32 random characters.  Generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-before-deploying-must-be-32-chars-minimum
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080   # 7 days

# ── Azure Key Vault [PROD ONLY] ───────────────────────────────────────────────
# The Container App's Managed Identity must have "Key Vault Secrets User" role.
AZURE_KEY_VAULT_URL=https://your-keyvault-name.vault.azure.net/
COSMOS_DB_SECRET_NAME=cosmos-db-connection-string

# ── Azure Blob Storage [PROD ONLY] ────────────────────────────────────────────
AZURE_STORAGE_ACCOUNT_URL=https://yourstorageaccount.blob.core.windows.net
AZURE_STORAGE_CONTAINER=dharma-media

# ── CORS ──────────────────────────────────────────────────────────────────────
# Use ["*"] only in development.  In production, list explicit origins:
# ALLOWED_ORIGINS=["https://dharma-ai.app","https://staging.dharma-ai.app"]
ALLOWED_ORIGINS=["*"]
