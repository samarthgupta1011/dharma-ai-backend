# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1          # ASGI server (includes websockets + httptools)

# ── Database (Cosmos DB via MongoDB-compatible API) ────────────────────────────
beanie==1.27.0                     # Async ODM built on Motor + Pydantic v2
motor==3.6.0                       # Async Cosmos DB driver (wraps PyMongo)
# pymongo is intentionally unpinned — motor 3.6.0 requires pymongo>=4.9,<4.10
# and will pull in the correct version automatically as a transitive dependency.
pymongo>=4.9,<4.10                 # Constrained by motor 3.6.0; IndexModel is here

# ── Data Validation & Settings ────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1           # BaseSettings with .env file support

# ── Authentication ────────────────────────────────────────────────────────────
PyJWT==2.10.1                      # HS256 JWT encode / decode

# ── Azure SDK (production only — harmless to install in dev) ──────────────────
azure-identity==1.19.0             # DefaultAzureCredential (Managed Identity)
azure-keyvault-secrets==4.9.0      # SecretClient for Key Vault access
azure-storage-blob==12.24.0        # BlobServiceClient, generate_blob_sas, UserDelegationKey

# ── HTTP Client (for optional outbound calls, e.g. SMS provider) ──────────────
httpx==0.28.1

# ── Multipart form data ───────────────────────────────────────────────────────
python-multipart==0.0.20
